{
        {$CADDY_GLOBAL_OPTIONS}

        frankenphp {
                # worker /app/public/index.php
                {$FRANKENPHP_CONFIG}
        }
}

{$CADDY_EXTRA_CONFIG}

{$SERVER_NAME:localhost} {
        root * public/
        encode zstd br gzip

        {$CADDY_SERVER_EXTRA_DIRECTIVES}

        php_server
}
